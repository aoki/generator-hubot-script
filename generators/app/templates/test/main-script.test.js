var assert = require('power-assert');
var Strcolor = require('../scripts/<%= name %>');

describe('Sample test', function() {
  it('Sample test', function(done) {
    assert(1 === 1);
    done();
  });
});
